# Shared pytest fixtures can land here. Pytest auto-discovers this file at
# import time, so anything defined at module scope (e.g. @pytest.fixture
# functions) becomes available to every test in this tree without manual
# imports. Left empty intentionally for now.
